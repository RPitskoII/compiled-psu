# ── Required ──────────────────────────────────────────────────────────────────
# Anthropic Claude — used for ICP normalization and email generation
ANTHROPIC_API_KEY=sk-ant-...

# ── Optional ──────────────────────────────────────────────────────────────────
# Apollo.io — when set, the app fetches real leads from Apollo instead of mock data.
# Requires a Master API key (create one at https://app.apollo.io/#/settings/integrations/api).
#
# Credit cost per request:
#   - People search:        FREE (uses /mixed_people/api_search)
#   - Org enrichment:       ~1 org credit per unique company (to get tech stack + funding data)
#   - Typical cost: ~5 org credits for a single "Generate" click with 5 leads
#
APOLLO_API_KEY=your-apollo-master-api-key
